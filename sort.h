#ifndef H
#define H

#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void bubble_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif
